# 変更履歴

このアプリのバージョン番号・リリースノートを記録する。[Semantic Versioning](https://semver.org/lang/ja/) に準拠する（`メジャー.マイナー.パッチ`）。

- 新機能・仕様変更 → マイナーバージョンを上げる
- 不具合修正 → パッチバージョンを上げる
- 破壊的な仕様変更（旧URL・旧データ形式が使えなくなる等） → メジャーバージョンを上げる

**画面の作り替え・導線の移動・アイコンや配色の統一は、見た目の変化が大きくてもパッチ**にする。
判断基準は「利用者にできることが増えたか」。増えていないならマイナーは上げない。
（2026-08-15 に判断を誤って 2.9.0 を切り、2.8.3 に振り直した）

各エントリの詳細な技術的経緯は `improvement_list/` の対応するファイルを参照。

## 書き方の約束（重要）

このファイルはアプリ内の更新履歴ページ（`/release-notes`）にそのまま表示され、**スタッフ（アルバイトを含む）が読む**。
表示側は次の2つのルールだけで技術的な記述を落とすので、それに合わせて書くこと。

- **インデントした入れ子の箇条書きは表示されない** → 開発者向けの補足はここに書く
- **バッククォートで囲んだ部分は表示されない** → ファイル名・テーブル名・クラス名は必ず囲む

つまり、**トップレベルの箇条書きは「スタッフが読んで意味が分かる日本語」だけにする**。

---

## [2.23.1] - 2026-09-11

### 修正
- **店舗IDを変更したあと、一覧に古い値が表示され、「ログインURLをコピー」が存在しないURLを配る問題を修正しました**
  - 店舗IDは入力値の末尾にランダムな文字が付くが、保存後の画面が**入力値のまま**表示していた。APIが確定した値を返すようにし、画面はそれを使う
  - 変更後に「店舗IDは〇〇になりました。古いURLは使えなくなります」と案内を出すようにした（新規作成には元からあった案内が、編集には無かった）

## [2.23.0] - 2026-09-11

### 変更
- **店舗IDを変更しても、ログイン中のスタッフがそのまま使えるようになりました。** これまでは変更すると、全員が「別のアカウントでログイン中です」という画面を経由し、進むと行き止まりになっていました
  - セッションCookieに古い店舗IDが残り、`proxy.ts` が古いURLへ引き戻していた。45分ごとのセッション再確認で店舗IDも合わせ直すようにした（`refreshStoreSlug`）
  - 貼り直しの判定を `sessionCookieNeedsRefresh` に切り出しテストで固定。**比較項目が1つ漏れるだけで「直したのに直らない」状態になる**ため
  - ログイン画面の自店判定を、店舗IDの文字列だけでなく店舗の実体でも照合するようにした

### 修正
- スタッフに配る使い方の文面を、公開リポジトリから取り除きました
  - `docs/staff-line-note.txt` に本番URL・店舗ID・管理者の姓が入ったまま 2026-08-17 から公開されていた。そのURLはログイン不要でシフト表が見える画面に直結していた。`資料/`（git管理外）へ移動
  - **`npm run check:leak` を追加しCIに組み込んだ**（`scripts/check-public-leak.ts`）。本番URLと具体的な店舗IDを含むパスを検出して落とす。同じファイルを置き直すと実際に落ちることを確認済み
  - 経緯と、削除だけでは直らない理由は `docs/SECURITY.md`

## [2.22.2] - 2026-09-11

### 修正
- 要望が開発者に届かなかったときの記録に、原因を切り分けるための情報を足しました
  - GitHubが404を返す場合、「トークンの権限不足」と「環境変数の差し替えが未反映」が同じ症状になる。エラーの記録にトークンの**種類**（classic / fine-grained）だけを載せて区別できるようにした。**値は一切含めない**（接頭辞はGitHubが公開している書式）

## [2.22.1] - 2026-09-11

### 修正
- 要望が開発者に届かなかったとき、**本部の「エラーの記録」に必ず残る**ようにしました。これまでは届かなくても誰も気づけない場合がありました
  - `GITHUB_TOKEN` 未設定の経路が例外を投げずに `null` を返していたため、呼び出し側の `catch` に入らず記録もされなかった。投げるように変更
  - 記録を `void` ではなく `await` するようにした。サーバーレスでは応答後に実行が凍結されうるため、投げっぱなしだと書かれずに終わることがある

## [2.22.0] - 2026-09-11

### 追加
- **シフトを入力できる時間帯を、店舗ごとに設定できるようになりました。** これまでは 8:00〜22:00 で固定でした
  - 「シフト種別」の画面に「受付時間帯」を追加（点検項目 FN-9）。**既定は 8:00〜22:00 のままなので、いまの店舗は何も変わりません**
  - 時間帯を変えると、シフト種別で選べる時刻とシフト表の縦軸がそれに追従します
  - 時間帯を狭めるときは、すでに入っているシフトが何件表示範囲から外れるかを先に出してから保存します
  - 日をまたぐシフトは登録できません（終了は24:00まで）。深夜営業は今回の対象外という判断
  - 店舗ごとの設定を読む新しいRPC `get_store_public_settings` を追加（`supabase/migrations/2026-09-11b_store_public_settings.sql`）。**マイグレーション→デプロイの順で適用すること**（逆にすると、設定済みのシフト種別が一時的に既定値に戻って見える）

## [2.21.0] - 2026-09-11

### 変更
- **要望の「開発者へ」の送信先を、非公開の置き場所に移しました。** これまでは誰でも読める場所に本文がそのまま残る状態でした
  - 送信先を専用の非公開リポジトリ `flshwrks/shift-app-feedback` に変更し、**コードの既定値ごと差し替えた**ので、設定を忘れても公開側には落ちない（F-12）。⚠️ `GITHUB_TOKEN` が新リポジトリに届くかの初回確認が必要（`docs/OPERATIONS.md` §5-5）
  - Issue作成に失敗したとき、これまでサーバーログにしか出ず誰も気づけなかった。本部の「エラーの記録」に出るようにした
- **シフトのコメントが同じ店舗の人に見えることを、書く場所に明記しました。** 入力欄・ヘルプ・詳しい使い方の3か所
  - 2026-08-25 に入れた「コメントを本文表示する」機能は**仕様として維持**する判断（2026-09-11）。書く側が知らないまま私的なことを書けるのが問題なので、ヘルプだけでなく入力欄にも出した
- **本部管理者のログインが、名前を入力する方式になりました。** これまでは登録者の一覧から選ぶ形でした
  - ログイン画面が全本部管理者の氏名とIDを未認証で返していた（F-13）。店舗ログインは店舗IDの推測不能化(F-6)で守られているが、本部は固定パスで同等の防御が無く、**最強の権限の実名一覧だけが最も守りの薄い経路にあった**
  - `list_hq_admin_users()` の実行権限を revoke（`supabase/migrations/2026-09-11_hq_login_no_enumeration.sql`）。**デプロイ→マイグレーションの順で適用すること**
  - 存在しない名前とPINの誤りは同じ応答にしてある（名前の存在を確かめられないようにするため）

## [2.20.0] - 2026-09-11

### 変更
- **要望を「開発者へ」送るとき、その内容がお店の外に届くことを送信画面に表示するようになりました。** 同僚の名前やお客様とのやりとりは書かないでください
  - 送信先は環境変数 `GITHUB_FEEDBACK_REPO` で決まり、既定値が**公開**リポジトリのため、本文がインターネット上に残る状態だった。**非公開リポジトリへ切り替える運用対応が別途必要**（`docs/OPERATIONS.md` §5-5）。既存Issue 6件は開発者本人のテスト投稿のみで、スタッフの記述は未流出
- メニューの「ログアウト」を「本部管理へ戻る」から離し、取り違えにくくしました
- 薄すぎて読めなかった文字を濃くしました。特に**シフト希望でその日をまだ入力していないことを示す印**が見えやすくなっています
  - 罫線用の色 `text-slate-300` を文字に使っていた5箇所を `text-slate-500` へ
- 要望の削除確認が、他の画面と同じアプリ内の確認画面になりました
  - `window.confirm` の使用2箇所を廃止（`docs/DESIGN_SYSTEM.md` が禁じていた）
- 店舗の削除確認に「スタッフが1人でも残っていると削除できない」ことを先に表示するようになりました

### 修正
- **本部管理者のアカウントが、スタッフ管理の経路から削除・降格できてしまう問題を修正しました**
  - `/api/admin/users` の PATCH/DELETE が、hqロールに対して店舗境界の検査を省く際に**操作対象の種類まで絞らなくなっていた**（F-11）。前日 v2.19.0 で `/api/hq/admins` に入れた歯止め（再認証・自分は消せない・最後の1人は消せない）を素通りできた。判定を `isStoreScopedTarget()` に切り出しテストで固定
- ログイン画面が他サイトに埋め込まれるのを禁止しました（F-15）
  - `next.config.ts` に `X-Frame-Options` / `frame-ancestors` / `nosniff` / `Referrer-Policy` / `Permissions-Policy` を追加。本番起動して実際に付与されることを確認済み。CSP本体は意図的に見送り
- 依存パッケージの既知の脆弱性を解消しました（moderate 1件 → 0件）

## [2.19.0] - 2026-09-11

### 追加
- 本部管理者の追加・削除・PINの再発行のときに、**自分のPINをもう一度入力する**ようになりました。ログインしたままの端末を他の人が触っても、勝手に本部の権限を増やせないようにするためです。名前を変えるだけのときは聞かれません
  - `lib/reauth.ts` の `verifySelfSecret`。ログイン画面と同じ `verify_login` に通しているため、bcrypt照合と5回失敗ロック（15分）がそのまま効く。`developer` は合成ロールでDBに行が無いので `DEV_LOGIN_PASSWORD` と定数時間比較する
  - 対象は `POST` / `DELETE` / `PATCH`（PINを変える場合のみ）。名前だけの変更を対象外にしたのは、毎回求めると「とりあえず入力する」癖がついて確認の意味が薄れるため
- 本部管理者の画面に**いま何人いるか**が出るようになりました。4人以上のときは、覚えのない名前が混じっていないか確認をうながす案内が出ます
  - 人数の上限は設けていない。侵入経路は1つ作れれば足りるので上限は本来の脅威に効かず、正当な運用を妨げる害のほうが大きい。「止めるのではなく見えるようにする」方針（`docs/SECURITY.md`）

### 変更
- 追加のときの入力欄を「PIN」から「この人のPIN」に変え、自分の確認用PINと見分けやすくしました
  - 既知の弱点に **F-10（本部管理者の認証も4桁PIN）** を追加した。再認証を挟んでも守っているのは同じ4桁で、アカウントを増やせるようにした分だけ弱点も増える。`docs/OPERATIONS.md` §12 / §5-4、`docs/SECURITY.md`
  - `DEV_SECRET_MIN_LENGTH` を `lib/reauth.ts` に集約し、`app/api/dev-login` から重複した定数を削除した

## [2.18.0] - 2026-09-11

- **本部管理者アカウントを、画面から追加・編集・削除できるようになりました。**本部管理のナビに「本部管理者」が増えています
  - これまではデータベースを直接触るしかなく、増やすことも名前やPINを変えることもできませんでした
  - 安全のため、自分自身と最後の1人は削除できません。本部管理者が0人になると誰も本部管理画面に入れなくなるためです
  - 追加・削除・名前の変更・PINの再発行は、すべて操作の記録に残ります
  - 実装は `app/api/hq/admins/route.ts` と `lib/hqAdmins.ts`（歯止めの条件はテスト済み）
- **要望を「対応済み」にしても、オレンジの件数バッジが消えない不具合を直しました**
  - 対応済みへの更新はサーバー側の管理キーで行うため変更がクライアントに届かず、バッジだけが古い件数のまま残っていました。リロードすれば正しくなる状態でした

## [2.17.0] - 2026-09-10

- **詳しい使い方を全面的に作り直しました。**役割ごとにページが分かれ、目次と「いま何番を読んでいるか」が常に出ます
- どの役割のページも「さいしょに1回だけ／毎月やること／ときどきやること／困ったとき／早見表」の同じ並びになりました。他の役割のページを読まなくても、そのページだけで完結します
- 書かれていなかった操作を追加しました。公開シフト表、提出忘れを防ぐお知らせ、調整依頼の取り消し、スタッフの削除、操作の記録、エラーの記録など
- 紙に印刷できるようになりました。「よくある質問」の答えも印刷されます
- **店舗IDを編集したときに、推測されないためのランダムな文字が付かない不具合を直しました**
  - `PATCH /api/hq/stores` が `withRandomSuffix()` を通していなかった。`hasRandomSuffix()` で判定し、付いていない場合だけ付与する
  - 休憩控除の判定基準は「実働」ではなく「シフトの長さ（終了−開始）」。ガイドと `docs/GUIDE.md` の表記を実装に合わせた
  - ガイドは `/guide/[role]` の静的3ページ＋ハブ構成。メニューからはログイン中の役割のページへ直接遷移する

## [2.16.0] - 2026-09-10

- 「使い方（詳しい版）」を「詳しい使い方」に改称し、**アプリの中に移しました**。外部サイトを開かなくても読めます
- 内容を最新の仕様に更新しました。シフト種別が店舗ごとに設定できるようになった点、操作の記録・エラーの記録を追加しました
- どの店舗でも使える内容に書き直しました。特定の店舗のログインURLは載せず、「店頭のQRコードか、管理者から配られたリンクを使う」という書き方に変えています
  - 実装は `app/guide/`。ログイン不要（`proxy.ts` の matcher 対象外）で、店舗やスタッフの情報を含まない
  - 画面写真12枚は `public/guide/` に配置。テスト店舗のもので実在のスタッフ名は含まれない
- 在庫管理アプリについての記述を削除しました

## [2.15.0] - 2026-09-09

- **シフト種別の表示名や時間帯を設定しても、開き直すと元に戻ってしまう不具合を直しました**
  - 原因は読み取り側。`StoreProvider` は `AuthProvider` がJWTを取得し終える前に `app_settings` を読むため、RLSで0件になり既定値へ戻っていた。保存自体は成功していた
  - `get_shift_patterns` RPC（SECURITY DEFINER）を追加し、認証前でも読めるようにした。`app_settings` のポリシーは緩めていない
- シフト種別を、7件から**12件まで**追加できるようになりました
  - `shifts` / `shift_requests` のCHECK制約を A〜L に拡張（`migrations/2026-09-09_shift_type_slots.sql`）。既存の値はそのまま許可されるため既存データに影響はない
  - 12で止めているのは、記号ごとの色を見分けられる上限のため
- 初期状態はこれまでどおり A〜G の7件です。H以降は追加したときだけ出てきます

## [2.14.0] - 2026-09-09

- シフト種別を、追加・削除・並べ替えできるようになりました。設定画面の「シフト種別」から「編集する」に進んでください
- 並び順は、シフト入力の画面に出てくる順番になります
- 設定画面では中身の確認だけができます。編集は専用の画面に分けたので、他の設定を見に来て誤って触ってしまうことがなくなりました
  - 編集画面は `/s/[店舗ID]/admin/shift-types`。保存形式は順序つきの配列に変更（旧形式も読める）
  - 記号（A〜G）は `shifts.shift_type` の値そのものなので、並べ替えても振り直さない
- 削除した種別で入力済みのシフトはそのまま残り、時刻も従来どおり表示されます

## [2.13.0] - 2026-09-09

- シフト種別（A〜G）の時間帯を、店舗ごとに設定できるようになりました。設定画面の「シフト種別」から、開始・終了時刻と表示名（早番・通しなど）を変えられます
- 使わないシフト種別は選択肢から外せるようになりました。「使う」のチェックを外すと、シフト入力の画面に出なくなります
  - 保存先は `app_settings` の `shift_patterns`。未設定の店舗は従来のA〜Gが既定値になるため、移行作業は不要
  - 実装は `lib/shiftPatterns.ts` と `components/ShiftPatternEditor.tsx`。供給は `useShiftPatterns()`
- 設定を変えても、すでに入力済みのシフトの時刻は変わりません。次にその種別を選んだときの時刻が変わります

## [2.12.0] - 2026-08-31

- 店舗を追加するとき、店舗IDの末尾にランダムな6文字が自動で付くようになりました。ログインせずに見られる公開シフト表のURLを、部外者に推測されないようにするためです
  - 実装は `lib/storeSlug.ts`。サーバー側の `POST /api/hq/stores` で必ず付与する。点検項目 F-6 の一次対応
  - 2026-08-31 より前に作った店舗の店舗IDは推測できるままなので、改名が残作業（`docs/OPERATIONS.md` §5-3）
- 公開シフト表についてのヘルプの説明を、実際の仕様に合わせて書き直しました

## [2.11.0] - 2026-08-31

### 追加

- **不具合が起きたときの画面を用意しました。** これまでは真っ白な画面や英語のエラーが出るだけでしたが、日本語の案内と「もう一度読み込む」ボタンが出ます
- **起きた不具合が自動的に記録されるようになりました。** 本部管理の「エラー」タブで確認できます（本部管理者のみ）
  - 利用者の画面で起きたものと、サーバー側で起きたものの両方を拾う。同じ内容は1行にまとめて件数で表示する
  - 「画面が真っ白になった」と言われたときに、何が起きたのか調べられるようになった
- 未対応の件数はナビにバッジで表示されます

### 注意

- **メールなどの通知は届きません。** ログインしたときにバッジで気づく形です。週に一度は確認してください
  - 外部の監視サービスを使わず自前で記録している。委託先を増やさない判断（`docs/SECURITY.md`）
- **この機能を有効にするには、データベース側の準備が必要です**（`supabase/migrations/2026-08-31c_error_log.sql`）。準備前は画面にその旨が表示されます

## [2.10.2] - 2026-08-31

### 修正

- **本部管理者が店舗のスタッフ管理からスタッフを追加できなかった**のを修正しました。「storeIdを指定してください」と表示されて追加できない状態でした。店舗管理者では起きていません
  - 画面が `POST /api/admin/users` の body に `storeId` を送っていなかった。本部管理者は所属店舗を持たないため、追加先の店舗が決まらず400になっていた（多店舗対応から残っていた）
  - 店舗管理者の場合はサーバー側がセッションの店舗を強制適用するため、セキュリティ上の性質は変わらない

## [2.10.1] - 2026-08-31

### 修正

- **「操作の記録」で、休みのシフトが「00:00〜00:00」と表示されて読み取れなかった**のを修正しました。「休み」と表示されます
  - 休み（`off`）は時刻を持たないため、`detail` に `shift_type` を含めるようにした。追補のマイグレーション `supabase/migrations/2026-08-31b_audit_shift_type.sql` の適用が必要（未適用でも従来どおり動く）

## [2.10.0] - 2026-08-31

### 追加

- **「操作の記録」画面を追加しました。** 設定 →「操作の記録」から、誰がいつ何をしたかを新しい順に確認できます。管理者だけが見られます
  - 記録されるのは、シフトの確定・取消・時刻変更・削除と、スタッフの追加・削除・権限変更・名前変更・暗証番号の再発行、店舗の追加・削除
  - 権限の変更・スタッフの削除・暗証番号の再発行は赤字で表示し、後から問われやすい操作を目立たせている
- **記録は後から書き換えたり消したりできません。** 「勝手にシフトを変えられた」という相談があったときに確認するためのものです
  - RLSでSELECTポリシーだけを作り、書込みポリシーを作らないことで追記専用にしている。書けるのはDBトリガーと `service_role` のみ
- スタッフが自分の申請中シフトを直したり取り消したりした分は記録されません（本人の操作で、後から争いにならないため）

### 注意

- **この機能を有効にするには、データベース側の準備が必要です。** 準備前は画面にその旨が表示されます
  - `supabase/migrations/2026-08-31_audit_log.sql` を Supabase SQL Editor で適用する。コードとの適用順序は問わない

## [2.9.7] - 2026-08-31

### 修正

- **スタッフを削除すると、その人のログイン状態も切れるようになりました。** これまでは一覧から消しても、ログイン済みの端末では最大30日間シフト表を見ることができてしまいました
  - `lib/sessionGuard.ts` を新設し、APIとトークン再発行の入口で `users` を照合する。削除済みなら403/401、管理操作は即座に、閲覧も最長45分で停止する
- **権限を変更すると、その場で反映されるようになりました。** 管理者からスタッフに変えた人が、そのまま管理操作を続けられる状態がなくなりました
- 使用中にアカウントが削除・変更された場合は、ログイン画面に戻って理由が表示されます
  - サーバーの不調（DB障害など）と「セッションが無効」は区別しており、通信が一瞬切れただけでログアウトさせることはない

## [2.9.6] - 2026-08-31

### 修正

- **本部管理者としてログイン中に店舗のログイン画面を開くと、何も言わずに本部の画面へ戻されていた**のを修正しました。今は「別のアカウントでログイン中です」と表示し、そのまま続けるか、ログアウトしてその店舗でログインし直すかを選べます
  - 原因は `app/s/[storeSlug]/login/page.tsx` の「ログイン済みなら `homePathFor()` へ `router.replace`」が、自店・他店・本部を区別していなかったこと。自店のログイン画面に自店の人が来た場合（QR・ブックマークからの再訪）だけ従来どおり素通しする
- **1つのブラウザで同時に2つのアカウントにはログインできません**（ログイン状態は全タブで共有されます）。2つ並べて確認したいときは、シークレットウィンドウか別のブラウザをお使いください

## [2.9.5] - 2026-08-31

### 改善

- **アプリが止まったときに備えて、データを手元に取っておける仕組みを用意しました。** 画面の見た目や操作は変わりません
  - `npm run backup` で全テーブルをJSONに書き出す（`scripts/backup.ts`）。`users.pin_hash` は除外し、出力先 `backups/` は `.gitignore` 済み
  - サービスが止まったとき・データを失ったときの手順を `docs/OPERATIONS.md` §9 に追加（症状別の対応、平常時の備え、クラウドを失った場合の移行手順、復旧訓練）
- **間違った変更が公開されないように、自動テストを追加しました**
  - `tests/` に44件。実働時間の計算（休憩の控除の境界）、ログイン後の行き先、セッションCookieの改ざん・期限切れ・旧形式の拒否、DB用トークンのクレーム
  - `npm test`（Node標準のテストランナー＋`tsx`、依存の追加なし）。CIにも組み込み、通らない変更は本番に出ない
- 必要な設定が揃っているかを確認する `npm run check:env` を追加

## [2.9.4] - 2026-08-29

### 修正

- **開発者用のログインを、短い数字のパスワードから長いパスワードに変更しました。** スタッフのみなさんの操作は変わりません
  - `POST /api/dev-login` は4桁の数字1つで `developer`（全店舗横断）のセッションが取れ、試行回数の制限も記録も無かった（点検項目 F-1）
  - 入力欄をPINパッドからパスワード入力に変更し、24文字未満の設定値は503で拒否、同一IPから10分5回で429、成否をログに記録するようにした
  - 経緯は `docs/SECURITY.md`「開発者ログインの強化」、詳細は `improvement_list/2026-08-29_dev_login_hardening.md`
- 何度も間違えると一時的にログインできなくなります。しばらく待ってからやり直してください

## [2.9.3] - 2026-08-29

### 修正

- **アプリの土台になっているソフトウェアを、修正版に入れ替えました。** 見た目や操作は変わりません。安全に使い続けるための更新です
  - `next` 16.2.4 → 16.3.3、`@supabase/supabase-js` 2.104 → 2.112。`npm audit` の high 5件（Proxy/Middlewareのバイパス、`ws` のメモリ開示ほか）が 0 件になった
  - `npm audit` を CI に追加し、既知の脆弱性を含むまま気づかずデプロイされない状態にした

## [2.9.2] - 2026-08-25

### 改善

- **シフト表のコメントが、時間帯のすぐ下に文字として表示されるようになりました**。これまでは白い丸印が付くだけで、内容を読むにはシフトをタップする必要がありました
  - 対象は `components/TableView.tsx` と `components/TimelineView.tsx`。シフト確認・公開シフト表・管理者のシフト作成画面すべてに反映される
- 長いコメントは2行までで省略されます。全文はこれまでどおり、シフトをタップすると開いて読めます
  - 表形式は `line-clamp-2` ＋ `max-w-[96px]` で列幅が広がらないようにしている。タイムラインはブロックが小さくて文字が入らない場合（幅56px未満・高さ46px未満）だけ、従来の白丸表示を残す

## [2.9.1] - 2026-08-17

### 修正

- **本部管理者が店舗の画面からログアウトすると、本部のログイン画面ではなくその店舗のログイン画面に飛ばされていた**のを修正。本部の画面に戻りにくくなっていた
  - `logout()` で利用者情報が消えた瞬間に、店舗レイアウトの「未ログインなら店舗のログイン画面へ」という `router.replace` が後から発火し、ログアウト処理が決めた遷移先を上書きしていた。ログアウト後はロールが分からないため、レイアウト側では本部管理者だと判別できない。完全な遷移に変えて、クライアント側のルーティングに割り込まれないようにした

### 追加

- **店舗のログイン画面から本部のログイン画面へ行けるようにした**。左下に控えめなリンクを置いてある。URLを覚えていなくても戻れる

---

## [2.9.0] - 2026-08-17

### 追加

- **使い方の完全版サイトを追加した**: 画面右上のメニュー（≡）の「使い方（詳しい版）」から開けます。スタッフ・店舗管理者・本部管理者それぞれの手順を、アプリ内のヘルプより詳しくまとめた読み物です
  - アプリ内ヘルプ = 操作中に開く早見表、完全版 = 最初に通して読む説明、という分担。URLは `lib/routes.ts` の `FULL_GUIDE_URL`
  - シフト種別・実働時間の計算・提出期間のロジック・権限の一覧など、アプリ内では簡潔にしか書けなかったことを収録している

---

## [2.8.3] - 2026-08-15

### 修正

- **設定から人件費予測・アンケート管理・要望へ移動すると、画面の先頭がヘッダーの裏に隠れた状態で始まっていた**のを修正
  - App Router は遷移時に新しいページ要素を `scrollIntoView` するため、`sticky` なヘッダーの裏に潜り込んでいた。遷移のたびにページ先頭へ戻す `ScrollToTop` を各レイアウトに置いた

### 変更

- **人件費予測・アンケート管理・要望に「設定へ戻る」を付けた**: これらはナビに項目が無く、戻る手段がブラウザバックしかなかった
  - 人件費予測にあった戻るは `router.back()` で戻り先が状況によって変わっていたため、設定へ戻る形に統一した
- **画面に残っていた絵文字をすべて線画アイコンに置き換えた**: 一覧が空のときの表示、承諾・辞退の状態、注意書き、コピー完了・提出完了の印など
  - 空表示は `EmptyState` に共通化。`IconX` / `IconAlertTriangle` を追加し、他は既存アイコンを再利用

---

## [2.8.2] - 2026-08-15

### 変更

- **使い方の画面のアイコンを線画に統一した**: 人件費予測・アンケート管理・要望・店舗管理などが絵文字のまま表示され、他の項目の線画アイコンと混ざっていた
  - `Section.icon` を絵文字そのものから `SectionIconName`（`'pencil'` 等の名前）に変更し、`SECTION_ICONS` を `Record<SectionIconName, ...>` にした。名前を増やしてSVGの割り当てを忘れるとコンパイルエラーになる。以前は対応表に無い絵文字が素のまま描画されていた

---

## [2.8.1] - 2026-08-15

### 修正

- **設定のシフト提出期間で「保存」ボタンが画面からはみ出していた**のを修正。日付入力と保存ボタンを同じ行に並べていたため、狭い画面で収まりきらなかった
  - 日付入力に `min-w-0` が無く、`type="date"` が持つ最小幅より縮めなかったのが原因。操作ボタンは日付の下の行へ移した

### 変更

- **設定のシフト提出期間をコンパクトにした**: 手前の2ヶ月だけを常に表示し、先の月は「先の月も設定する」で開くようにした。6ヶ月分を常に並べていたため画面が縦に長すぎた
  - 保存・解除のボタンは、変更があるときと設定済みのときだけ表示する
- **設定の各メニューを見分けやすくした**: 人件費予測・アンケート管理・要望が同じ見た目の白いボタンで区別しづらかったため、色分けしたアイコンを付けて「管理メニュー」としてまとめた
- **ヘッダーに名前を戻した**: 店舗名と名前だけを表示する（アプリ名はブランドマークで足りるので文字では出さない）

---

## [2.8.0] - 2026-08-15

### 追加

- **日付が過ぎた調整依頼を自動で削除**: 依頼管理の画面を開いたときに、過去日の依頼をまとめて片付けるようにした。募集中のまま日付だけ過ぎた依頼が残り、スタッフ側の未対応バッジが減らなくなるのを防ぐ
  - 依頼を消しても、承諾によって作成済みのシフトは残る。「今日」は端末の時計ではなくサーバー側で日本時間として求める
- **対応済みの要望を削除できるようにした**: 店舗・本部どちらの受信箱でも、対応済みのものだけ削除できる。未対応のものは削除できない（読まずに消せてしまわないようにするため）

### 変更

- **「要望」をナビから外し、設定の中に移した**: 毎日使うシフト・依頼・スタッフと同じ高さに並べると重要度を取り違えるため。未対応の要望があるときは「設定」に件数のバッジが出るので見落とさない
- **ヘッダーを整理した**: ユーザー名・ログアウト・本部管理へ戻るを右上のメニューにまとめ、ヘッダーには店舗名だけを残した。狭い画面で店舗名が潰れたり、本部管理者で文字が重なって読めなくなっていた
  - 狭い画面ではアプリ名「シフト管理」を省いて店舗名に幅を譲る（ブランドマークで識別できる）
- **使い方の並びを画面構成に合わせた**: 人件費予測・アンケート管理・要望を「設定から開くもの」として整理した
- 本部の受信箱で、GitHub Issueへのリンクを本文の下から上（種別・店舗と同じ行）へ移した。どのIssueかは本文より先に知りたい情報のため

---

## [2.7.0] - 2026-08-15

### 追加

- **本部管理に「要望」を追加**: 全店舗のスタッフが「開発者へ」宛てに送った要望を、本部管理者がまとめて確認できるようになった。どの店舗の誰が、どのバージョンで送ったかが分かり、GitHubに起票されていればそこへ移動できる
  - 新規ページ `app/admin/feedback/page.tsx`。`destination = 'dev'` を全店舗横断で表示する。RLS上、本部管理者は元から全要望が見える
  - 本部管理エリアにナビ（店舗一覧 / 要望）を追加。未対応件数のバッジ付き

### 変更

- **要望の置き場を宛先ごとに完全に分けた**: 「管理者へ」は各店舗の管理画面、「開発者へ」は本部管理。同じ画面に混ざらないようにし、それぞれの画面に「ここに何が届くのか」を明記した

### 修正

- 受信箱で「未対応」タブが空のとき、対応済みの要望があってもそれに気づけず「要望が確認できない」状態になっていた。対応済みの件数を空表示に出し、そこから切り替えられるようにした
- 受信箱の取得エラーを画面に出さずに握りつぶしていたため、「0件」と「読み込めていない」が区別できなかった

---

## [2.6.1] - 2026-08-15

### 修正

- **画面右上のメニューを開いても中身が正しく表示されなかった不具合を修正**。「使い方」も同じ理由で開けなくなっていた
  - ヘッダーに `backdrop-filter`（`backdrop-blur`）が掛かっており、その子孫の `position: fixed` はビューポートではなくヘッダーを包含ブロックとして配置される。メニューとモーダルを `createPortal` で `body` 直下に描くよう変更した

### 変更

- メニューのアイコンを「⋯」からハンバーガー（≡）に変更した

---

## [2.6.0] - 2026-08-15

### 追加

- **公開シフト表にメモを表示**: ログイン不要で見られるシフト表にも、日付ごとのメモが出るようになった
  - `get_public_memos` RPC を新設し、`app_settings` の `memo_YYYY-MM-DD` だけを匿名公開する。提出期間・時給など他のキーは返さない
  - Ver.2.3.0 の「メモは公開しない」という判断を意図的に覆している。URLを知っていれば誰でも読めるため、メモに個人的な内容を書かない運用が前提

### 変更

- **使い方・要望を送る・更新履歴を1か所にまとめた**: 画面右上のメニューから開く。画面下部のフッターは廃止した。長いシフト表の一番下までスクロールしなくても届くようになった
- **要望を送れるのはスタッフだけにした**: 管理者・本部管理者は要望を受け取って対応する側なので、送る導線を出さないようにした
- **要望の送信先の呼び方を「管理者へ」に統一**
- **更新履歴を読みやすくした**: 種類（追加・変更・修正）を色分けし、開発者向けの技術的な記述は表示しないようにした

### 修正

- 要望を「対応済み」にした後も操作ボタンや状態表示が中途半端に残り、対応が済んだのかどうか分かりにくかった。**未対応・対応済みの2つ**に整理し、間違えたときは「未対応に戻す」で戻せるようにした
  - 内部的には未読/既読/対応済みの3状態のままだが、画面上は `done` かどうかだけで扱う。ナビの件数バッジも受信箱のタブと一致するよう「未対応の件数」に揃えた

---

## [2.5.0] - 2026-08-14

要望を送れるようにした。あわせて、アプリ内ヘルプと使い方ガイドがズレない仕組みに変えた。

### 追加

- **要望の送信**: 要望・不具合を送れるようになった。送信先は「管理者へ」（お店の管理者に届く）と「開発者へ」（アプリの改善要望として届く）から選ぶ
- **要望の受信箱**: 店舗管理者のナビに「要望」が増えた。届いた要望を確認し、対応済みにして整理できる
- **更新履歴ページ**: これまでの全バージョンの変更内容を読めるようにした。ログイン不要で開ける
- **バージョン表示**: 現在のバージョンを確認できるようにした
- **更新のお知らせ**: バージョンが上がったとき、最初のログイン時に画面下部へ数秒だけ通知を出す。操作は不要で自動的に消える（見逃しても「更新履歴」から読める）

### 変更

- **本部管理者に、本部向けのヘルプが表示されるようになった**（従来は店舗管理者向けのヘルプが出ていた）
  - アプリ内ヘルプと使い方ガイド（`docs/GUIDE.md`）を `lib/help/content.ts` から生成する方式に変更。CIで両者のズレを検出する

### 修正

- アプリ内ヘルプの「組織名を入力して保存」という説明が、複数店舗対応後の実際の動き（店舗名は本部管理者が変更する）と食い違っていた
- アプリ内ヘルプにアンケート機能・人件費予測・公開シフト表の説明が無かった
- 使い方ガイドに載っていた「提出期間外はロックされる」「確定済みのシフトは編集できない」という説明が、アプリ内ヘルプ側に無かった

---

## [2.4.0] - 2026-08-13

在庫管理アプリと配色・書体を統一した。機能の増減はない。

### 変更

- **配色を一新**: 全体をやや寒色寄りの落ち着いた色に。文字と背景のコントラストを上げ、屋内の明るい場所でも読みやすくした
- **数字が読みやすくなった**: 日付・時刻・人数・時給に専用の書体を当てた。日本語は従来どおりで、追加の読み込みは発生しない
- **角の丸みと影を控えめに**: 情報の区切りを影ではなく罫線で示すようにし、1画面に入る情報が増えた

### 修正

- エラー文の赤が薄く、小さい文字で読みづらかった
  - `text-red-500` のコントラスト比が 3.76:1 で WCAG AA 未達だった
- 「承認済み」等の緑も同様に薄く読みづらかった
  - `text-emerald-600` 3.40:1 / `text-green-600` 3.03:1

---

## [2.3.0] - 2026-08-09

### 追加

- **ログイン不要のシフト閲覧を追加**: 店舗ログイン画面から、PIN入力なしでシフト表を見られるようになった。休憩室のタブレット等で1タップで確認する用途を想定している。表形式・タイムライン両方に対応し、提出・編集・確定などの操作は一切できない完全な読み取り専用
  - 公開ページのパスは `/s/[storeSlug]/public/schedule`
  - コメント欄・メモは表示しない（不特定多数に見えるべきでない私的な内容のため）※メモはVer.2.6.0で表示するよう変更
  - RLSによる店舗間の遮断はそのまま維持し、`get_public_shifts` という新設のSECURITY DEFINER RPCで必要な範囲だけを匿名公開する形にした
- **注意**: このページのURLを知っていれば誰でも（ログインなしで）その店舗のスタッフ名とシフト予定を見られる。店頭掲示・共有時はこの点を踏まえること

### 変更

- シフト確認画面の表形式で、日ごとの「出勤人数」表示を「実働時間」（休憩控除後の合計稼働時間）表示に変更

---

## [2.2.0] - 2026-08-09

### 追加

- **本部管理者が店舗を行き来しやすくなった**: 店舗の管理画面を見ている間、ログアウトせずに本部の店舗一覧へ戻れる「本部管理へ戻る」リンクをヘッダーに追加。従来はログアウトしてPINを入れ直す必要があり、店舗を行き来するたびに手間がかかっていた

---

## [2.1.1] - 2026-08-09

### 修正

- 店舗を編集したときに、「保存はできたが実際には何も変わっていない」という状態になり得た不備を修正
  - `/admin/stores` で対象idが見つからない場合でもエラーにならなかった

---

## [2.1.0] - 2026-08-09

### 修正

- 本部管理者が店舗の管理画面を見ている最中にログアウトすると、本部のログイン画面ではなくその店舗のログイン画面に戻ってしまう不具合を修正
  - ロールを先に判定し、本部管理者は必ず `/admin/login` へ戻すようにした

---

## [2.0.0] - 2026-08-09

複数店舗に対応した。1店舗だけの運用から、同じ会社の複数店舗＋本部管理へ広げた。

### 追加・変更（大きな変更）

- **店舗という単位ができた**: スタッフ・管理者は1人1店舗に所属する（掛け持ちはできない）
- **本部管理者が追加された**: どの店舗にも属さず、全店舗をまとめて管理できる
- **ログインURLが店舗ごとになった**: 店頭にQRコードを掲示する運用を想定している。古いURLを開いた場合は自動的に新しいURLへ転送される
- **本部管理者専用のログイン画面と店舗管理画面を追加**
- 店舗どうしのデータが混ざらないことを、データベース側で保証するようにした
  - 店舗間の遮断をRLSで強制。httpOnly Cookieセッションはそのまま維持し、そこから派生する短命なJWTでRLSを機能させる形にした
  - 新URLは `/s/[storeSlug]/login`、旧URL（`/login`, `/staff/*`, `/admin/schedule` 等）からは自動転送
