# 変更履歴

このアプリのバージョン番号・リリースノートを記録する。[Semantic Versioning](https://semver.org/lang/ja/) に準拠する（`メジャー.マイナー.パッチ`）。

- 新機能・仕様変更 → マイナーバージョンを上げる
- 不具合修正 → パッチバージョンを上げる
- 破壊的な仕様変更（旧URL・旧データ形式が使えなくなる等） → メジャーバージョンを上げる

各エントリの詳細な技術的経緯は `improvement_list/` の対応するファイルを参照。

---

## [2.3.0] - 2026-08-09

### 追加
- 店舗ログイン画面から、PIN入力なしでシフト表を閲覧できる公開ページ（`/s/[storeSlug]/public/schedule`）を追加。休憩室のタブレット等で1タップで確認できる用途を想定。提出・編集・確定などの操作は一切できない完全な読み取り専用。表形式・タイムライン両方の表示に対応
  - コメント欄・メモは表示しない（不特定多数に見えるべきでない私的な内容のため）
  - RLSによる店舗間の遮断はそのまま維持し、`get_public_shifts` という新設のSECURITY DEFINER RPCで必要な範囲だけを匿名公開する形にした（テーブルへの直接アクセスは引き続きブロックされる）
  - **仕様上のトレードオフ**: このページのURLを知っていれば誰でも（ログインなしで）該当店舗のスタッフ名とシフト予定を閲覧できる。店頭掲示・共有時はこの点を踏まえること

### 変更
- シフト確認画面の表形式ビューで、日ごとの「出勤人数」表示を「実働時間」（休憩控除後の合計稼働時間）表示に変更

## [2.2.0] - 2026-08-09

### 追加
- 本部管理者が店舗の管理画面を見ている間、ログアウト（PIN再入力）せずにセッションを保ったまま本部の店舗一覧(`/admin/stores`)へ戻れる「本部管理へ戻る」リンクをヘッダーに追加。従来はログアウト→本部ログインのPIN再入力が必要で、店舗を行き来するたびに工数がかかっていた

## [2.1.1] - 2026-08-09

### 修正
- 店舗管理画面(`/admin/stores`)で店舗を編集した際、対象idが見つからない場合でもエラーにならず「保存はできたが実際には何も変わっていない」状態になり得た不備を修正

## [2.1.0] - 2026-08-09

### 修正
- 本部管理者が店舗の管理画面（`/s/[storeSlug]/admin/*`）を閲覧中にログアウトすると、本部ログイン(`/admin/login`)ではなくその店舗のログイン画面に戻ってしまう不具合を修正。ロールを先に判定し、本部管理者は必ず本部ログインへ戻すようにした

## [2.0.0] - 2026-08-09

### 追加・変更（破壊的）
マルチ店舗対応。単一店舗運用から、同一組織内の複数店舗＋本部管理へ拡張した。

- 店舗（`stores`）という概念を新設。スタッフ・管理者は1人1店舗（掛け持ちなし）に所属
- 本部管理者（`hq_admin`）ロールを新設。店舗に紐づかず、全店舗を横断管理できる
- ログインURLが店舗ごとの専用URL（`/s/[storeSlug]/login`、店頭QRコード掲示を想定）に変更。旧URL（`/login`, `/staff/*`, `/admin/schedule` 等）は自動的に新URLへ転送される
- 本部管理者専用のログイン(`/admin/login`)・店舗管理画面(`/admin/stores`)を追加
- 店舗間のデータ遮断をDBレベル（RLS）で保証するよう認証方式を拡張。httpOnly Cookieセッションはそのまま維持し、そこから派生する短命なJWTでSupabaseのRLSを機能させる形にした

詳細: `improvement_list/2026-08-08_multi_store_support.md`
